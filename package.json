{
  "name": "canit",
  "version": "0.1.0",
  "private": true,
  "description": "A futuristic AI application arena ranking web experience.",
  "scripts": {
    "start": "python3 -m http.server 5173",
    "build": "node --check src/main.js && node scripts/validate-static-site.js"
  }
}
